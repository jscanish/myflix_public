- @categories.each do |category|
  %article.video_category
    %header
      %h3= link_to category.name, category_path(category)
    .videos.row
      - category.recent_videos.each do |video|
        .video.span2
          = link_to (image_tag(src="#{video.small_cover}")), video_path(video)

